# Secrets — never commit
.env

# Python cache
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store

# Raw dataset (45 MB — run ingest.py locally to regenerate artifacts)
data/

# Virtual environments
venv/
.venv/

# Claude Code local config
.claude/

# artifacts/ and *.pkl are intentionally INCLUDED so Railway can serve the API
# without re-running the heavy ingest pipeline on every deploy

